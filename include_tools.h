// Header file for tools.cpp

#pragma once

double* normalize(int* dist, int length, int n);
int* distribution(double* array, int N, double limit, double dx);
double difference(double* array1, double* array2, int n);
